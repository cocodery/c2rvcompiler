cmake_minimum_required(VERSION 3.16)
set(project_name compiler)
project(${project_name} CXX)
set(CMAKE_CXX_STANDARD 17)

file(GLOB_RECURSE source_files "src/*.cpp")
link_libraries("/usr/local/lib/libantlr4-runtime.a")
add_compile_options(-g -std=c++17 -O2)
include_directories(src)
include_directories(src/antlr)
include_directories(src/frontend)
include_directories(src/include)
include_directories(src/include/IR)
include_directories(src/include/IR/value)
include_directories(src/include/IR/instruction)
include_directories(src/include/IR/function)
include_directories(src/include/IR/value/type)
include_directories(src/optimization)
include_directories(src/optimization/interprocedural)
include_directories(src/optimization/interprocedural/deadCodeElim)
include_directories(src/optimization/interprocedural/dominance)
include_directories(src/optimization/interprocedural/getAllNodes)
include_directories(/usr/local/include/antlr4.12-runtime)
add_executable(compiler ${source_files})